/**
 * Apply universal column modifiers (default/nullable/unique/...) to a Drizzle
 * column builder. Stub — full logic lands in dispatch C/F/G.
 */
export function applyCommon(col: any, _node: any, _ctx: any): any {
  return col
}
