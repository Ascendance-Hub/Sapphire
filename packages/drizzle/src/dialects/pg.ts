import type { SapphireSchemaNode } from '@ascendance-hub/sapphire-core'

/**
 * Build a Postgres `pgTable` from a Sapphire ObjectField IR node.
 *
 * Stub — full implementation lands in dispatch C (primitives + composite jsonb)
 * and dispatch D (refs + registry).
 */
export function buildTable(_node: SapphireSchemaNode, _ctx: any): any {
  return {} as any
}
